# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# MyMemory Translation API — free, no signup, no credit card.
# This is OPTIONAL: without it, translation still works but is limited
# to 5,000 characters/day. Adding your email (just as plain text, no
# verification needed) raises the limit to ~50,000 characters/day.
MYMEMORY_EMAIL=
